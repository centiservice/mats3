// mats-api

dependencies {
    // None

    // === TEST
    testImplementation "junit:junit:$junitVersion"
}

mavenPublishing {
    pom {
        name = 'Mats^3 API'
        description = 'API for Mats^3: Message-based Asynchronous Transactional Staged Stateless Services.'
    }
}
